---
export interface Props {
	name: string;
	href: string;
}

const { name, href } = Astro.props;
---

{href ? <a {href}>{name}</a> : <span>{name}</span>}

<style>
	.author {
		/* margin-bottom: 0.75rem; */
	}
</style>
