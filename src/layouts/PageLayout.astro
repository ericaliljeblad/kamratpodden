---
import { SITE } from '../config';
import BaseHead from '../components/BaseHead.astro';
import BlogHeader from '../components/BlogHeader.astro';
import Footer from '../components/Footer.astro';

let { title, description, permalink } = SITE;
const { home = false, header } = Astro.props;
---

<html lang="en">
	<head>
		<BaseHead {title} {description} {permalink} />

		<style>

			/* .content {
				margin-top: 4rem;
				margin-bottom: 8rem;
			}

			.content :global(main > * + *) {
				margin-top: 1rem;
			} */

			/* .intro {
				padding-bottom: 4rem;
				margin-bottom: 2rem;
				border-bottom: 4px solid var(--theme-divider);
			}

			.intro > * {
				margin: 0;
			} */

			/* .latest {
				font-size: 2.5rem;
				font-weight: 700;
			} */

		</style>
	</head>

	<body>
    <BlogHeader home={home} header={header} />
    <main class="content">
      <slot />
    </main>

		<Footer />
	</body>
</html>